$(document).ready(function () {
    // Tìm tất cả thẻ a có giá trị của thuộc tính name có chứa chữ 'name'
    // $("a[name*='link']").css('color', 'red')
    
    // Tìm tất cả thẻ a có giá trị của thuộc tính class bắt đầu bằng chữ 'link'
    // $("a[class^='link']").css('color', 'yellow')
  
    // Tìm tất cả thẻ a có giá trị của thuộc tính id kết thúc bằng chữ 'link'
    // $("a[id$='link']").css('color', 'violet')

    // Tìm tất cả thẻ a có giá trị của thuộc tính value là 'link', phân biệt hoa thường
    // $("a[value='link']").css('color', 'yellow')

    // Tìm tất cả thẻ a có giá trị của thuộc tính value khác với chữ 'link'
    // $("a[value!='link']").css('color', 'grey')

})